namespace PluginBiometrico.Core.Modelos;

/// <summary>Estado del proceso de enrollment (registro de huella).</summary>
public enum EstadoEnrollment
{
    EnProgreso,
    PlantillaLista,
    Fallido
}
