/* Stub para `npm run build` sin HID Lite Client instalado en la máquina de CI.
   En producción/operador reemplace por websdk.client.ui.min.js del Lite Client. */
(function (global) {
  global.WebSdk = global.WebSdk || {
    WebChannel: function WebChannel() {},
  };
})(typeof window !== 'undefined' ? window : globalThis);
